PREFIX rel2kg: <https://rel2kg.example/vocab#>

# Named graphs in the dataset. Expected: 5 (hr, library, registrar, vocab, identity).

SELECT DISTINCT ?g
WHERE {
  GRAPH ?g { ?s ?p ?o }
  FILTER(STRSTARTS(STR(?g), "https://rel2kg.example/graph/"))
}
ORDER BY ?g
